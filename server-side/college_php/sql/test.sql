-- CREATE TABLE Customers (
    -- cid INT NOT NULL AUTO_INCREMENT,
    -- name VARCHAR(120) NOT NULL,
    -- age INT,

    -- PRIMARY KEY(cid)
-- );

INSERT INTO Customers (
    name, age
) VALUES (
    "Naman", 69
);
